def solution(land):
    answer = 0
    print(land)
    
    for i in range(len(land)):
        max_n = max(land[i])
        max_n_index = land[i].find(max_num)
        tmp = 0
        for j in range(4):
            
            if j != max_n_index:
                if tmp < land[i][j]
        # for j in range(len(land[0])):
           
    
    return answer